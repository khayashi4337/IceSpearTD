/**
 * ゲームの基本設定
 */
export const gameConfig = {
    /**
     * グリッドの設定
     */
    grid: {
        /**
         * グリッドセルのサイズ（ピクセル）
         * @type {number}
         */
        cellSize: 20,

        /**
         * エンティティの位置調整用オフセット
         * エンティティをセルの中央に配置するために使用
         * @type {number}
         */
        entityOffset: 0
    }
};
